import styled from '@emotion/styled';
import Layout from '../../components/Layout';

function Blog() {
  return (
    <Layout>
      <BlogBlock />
    </Layout>
  );
}

const BlogBlock = styled.div`
  
`;

export default Blog;
